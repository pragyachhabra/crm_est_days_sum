<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record model="ir.ui.view" id="crm_est_days_sum_inherit_crm_case_kanban_view_leads">
            <field name="name">crm.est.days.sum.inherit.crm.case.kanban.view.leads</field>
            <field name="model">crm.lead</field>
            <field name="inherit_id" ref="crm.crm_case_kanban_view_leads" />
            <field name="arch" type="xml">
                <field name="color" position="after">
                    <field name="est_days" />
                </field>
            </field>
        </record>

    </data>
</odoo>
